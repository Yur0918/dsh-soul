# dsh-soul v2 (SoulFusion) — bundle patch: inserts the plugin into a profile's
# layer stack. The client bundle (client/client.js) carries its own
# __ModuleLoader__.load registration; section/command/tool/API wiring is done
# by the host entry (lib/plugin.js, package.json main).
- insert:
    - id: soul
      name: '@yur/dsh-soul'
